import React from "react";
import { useParams } from "react-router-dom";
function MoviePage() {
    const {id} = useParams()
    
    return (
        <div>

        </div>
    );
}

export default MoviePage;
